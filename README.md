# Adriana's Blog

Adriana finally has a web site. Wheeeee!

Big thanks to [Cassidy Williams](https://github.com/cassidoo) for this [awesome blog template](https://github.com/cassidoo/blahg).

## Notes to self

Because I can never remember this stuff.

### Running locally


All commands are run from the root of the project, from a terminal:


| Command                          | Action                                                        |
| :------------------------------- | :------------------------------------------------------------ |
| `npm install`                    | Installs dependencies                                         |
| `npm run dev`                    | Starts local dev server at `localhost:4321`                   |
| `npx tinacms dev -c 'astro dev'` | Manually run local server if the regular command doesn't work |
| `npm run build`                  | Build your production site to `./dist/`                       |
| `npm run preview`                | Preview your build locally, before deploying                  |

